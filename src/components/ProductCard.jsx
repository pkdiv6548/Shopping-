import {Heart,ShoppingBag,Star} from "lucide-react";import {Link} from "react-router-dom";
export default function ProductCard({p,wish,onWish,onAdd}){return <article className="card"><div className="visual"><Link to={`/product/${p.id}`}><img src={p.images[0]} alt={p.name}/><img className="hover" src={p.images[1]} alt=""/></Link>{p.badge&&<span className="badge">{p.badge}</span>}<button className={"heart "+(wish?"on":"")} onClick={()=>onWish(p.id)}><Heart size={18} fill={wish?"currentColor":"none"}/></button><button className="quick" onClick={()=>onAdd(p)}>QUICK ADD <ShoppingBag size={15}/></button></div><div className="pinfo"><div className="meta"><span>{p.category}</span><span><Star size={11} fill="currentColor"/> {p.rating}</span></div><Link className="pname" to={`/product/${p.id}`}>{p.name}</Link><b>${p.price}</b><small>{p.color} · +{(p.id%4)+1} colors</small></div></article>}