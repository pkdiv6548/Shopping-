import {Link} from "react-router-dom";
export default function Wishlist({products,wish,onWish,onAdd}){const items=products.filter(p=>wish.includes(p.id));return <main className="page-shell"><div className="page-hero"><small>SAVED ITEMS</small><h1>Wishlist</h1><p>{items.length} saved {items.length===1?"item":"items"}.</p></div>{items.length?<div className="mini-grid">{items.map(p=><div key={p.id}><Link to={"/product/"+p.id}><img src={p.images[0]} alt={p.name}/><b>{p.name}</b></Link><button onClick={()=>onAdd(p)}>Add to bag</button><button className="link-btn" onClick={()=>onWish(p.id)}>Remove</button></div>)}</div>:<div className="empty-state"><p>Your wishlist is empty.</p><Link className="solid-btn" to="/category/Women">Explore new arrivals</Link></div>}</main>}
