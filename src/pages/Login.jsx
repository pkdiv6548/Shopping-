import {useState} from "react"; import {Link,useNavigate} from "react-router-dom";
export default function Login(){const [ok,setOk]=useState(false),nav=useNavigate();return <main className="auth-page"><div className="auth-card"><small>MY ATELIER</small><h1>Sign in</h1><form onSubmit={e=>{e.preventDefault();setOk(true);setTimeout(()=>nav("/account"),300)}}><input required type="email" placeholder="Email"/><input required type="password" placeholder="Password"/><button>Sign in</button></form><Link to="/forgot-password">Forgot password?</Link><p>New here? <Link to="/register">Create an account</Link></p>{ok&&<div className="notice success">Signed in for this demo.</div>}</div></main>}
