import {useState} from "react";
export default function Membership(){const [joined,setJoined]=useState(false);return <main className="page-shell"><div className="page-hero"><small>ATELIER MEMBERSHIP</small><h1>Member benefits</h1><p>Early access, saved preferences, member-only drops and tailored recommendations.</p></div><div className="benefit-grid">{["Early access","Member pricing","Birthday reward","Style profile","Priority support","Private events"].map(x=><article key={x}><b>{x}</b><p>Available in this demo experience.</p></article>)}</div><button onClick={()=>setJoined(true)}>{joined?"You're a member":"Join membership"}</button>{joined&&<div className="notice success">Membership activated.</div>}</main>}
