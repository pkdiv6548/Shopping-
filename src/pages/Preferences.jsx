import {useState} from "react";
export default function Preferences(){const [saved,setSaved]=useState(false),[prefs,setPrefs]=useState({new:true,offers:true,editorial:false});return <main className="page-shell"><div className="page-hero"><small>ACCOUNT</small><h1>Preferences</h1><p>Control the content and offers you receive.</p></div><div className="toggle-list">{Object.entries(prefs).map(([k,v])=><label key={k}><span>{k==="new"?"New arrivals":k==="offers"?"Offers & promotions":"Editorial stories"}</span><input type="checkbox" checked={v} onChange={()=>setPrefs(p=>({...p,[k]:!p[k]}))}/></label>)}</div><button onClick={()=>setSaved(true)}>Save preferences</button>{saved&&<div className="notice success">Preferences saved.</div>}</main>}
