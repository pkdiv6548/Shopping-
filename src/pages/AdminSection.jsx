import {useState} from "react";
export default function AdminSection({title}){const [saved,setSaved]=useState(false);return <main className="page-shell"><div className="page-hero"><small>ADMIN / CMS</small><h1>{title}</h1><p>Operational controls with mock data and backend-ready interaction points.</p></div><div className="admin-table"><div className="admin-head"><b>Record</b><b>Status</b><b>Action</b></div>{Array.from({length:6},(_,i)=><div className="admin-row" key={i}><span>{title} record #{100+i}</span><span>{i%2?"Active":"Review"}</span><button onClick={()=>setSaved(true)}>Update</button></div>)}</div>{saved&&<div className="notice success">Saved to demo state. Connect your API/database for persistence.</div>}</main>}
