import {useState} from "react"; import {Link,useNavigate} from "react-router-dom";
export default function Register(){const [ok,setOk]=useState(false),nav=useNavigate();return <main className="auth-page"><div className="auth-card"><small>MY ATELIER</small><h1>Create account</h1><form onSubmit={e=>{e.preventDefault();setOk(true);setTimeout(()=>nav("/account"),300)}}><input required placeholder="Full name"/><input required type="email" placeholder="Email"/><input required type="password" minLength="6" placeholder="Password"/><button>Create account</button></form><p>Already registered? <Link to="/login">Sign in</Link></p>{ok&&<div className="notice success">Account created for this demo.</div>}</div></main>}
