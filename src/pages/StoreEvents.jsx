import {useState} from "react";
export default function StoreEvents(){const [booked,setBooked]=useState(false);return <main className="page-shell"><div className="page-hero"><small>STORE EXPERIENCE</small><h1>Appointments & events</h1><p>Book a styling session or RSVP to an in-store event.</p></div><div className="event-grid">{["Private styling · Jaipur","New season preview · Delhi","Material workshop · Mumbai"].map(x=><article key={x}><b>{x}</b><p>Saturday · 4:00 PM</p><button onClick={()=>setBooked(true)}>Reserve</button></article>)}</div>{booked&&<div className="notice success">Reservation confirmed for this demo.</div>}</main>}
