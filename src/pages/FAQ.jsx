import {useState} from "react";
const qs=["How do I track my order?","Can I change or cancel an order?","What payment methods are supported?","How do returns work?","Can I find an item in a store?","How do I use a gift card?"];
export default function FAQ(){const [open,setOpen]=useState(null);return <main className="page-shell"><div className="page-hero"><small>HELP</small><h1>Frequently asked questions</h1><p>Fast answers for shopping, delivery, payment and account questions.</p></div><div className="faq-list">{qs.map((q,i)=><div className="faq-item" key={q}><button onClick={()=>setOpen(open===i?null:i)}>{q}<span>{open===i?"−":"+"}</span></button>{open===i&&<p>{q.includes("track")?"Open Track Order and enter your order number and email.":q.includes("payment")?"Cards, UPI, wallets and supported digital payment methods are available at checkout.":q.includes("returns")?"Use Returns to start a return request and select an eligible item.":q.includes("store")?"Use Store Locator to check nearby availability.":q.includes("gift")?"Gift Cards includes balance checking and demo redemption.":"Contact support as soon as possible; changes depend on order status."}</p>}</div>)}</div></main>}
