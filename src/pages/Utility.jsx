import {Link} from "react-router-dom";import ProductCard from "../components/ProductCard";
export function Simple({title,eyebrow="ATELIER"}){return <main className="section simple"><small>{eyebrow}</small><h1>{title}</h1><p>Enterprise-ready experience with connected commerce workflows and backend-ready interfaces.</p><div className="info"><div>Personalized shopping</div><div>Flexible delivery & returns</div><div>Considered materials</div></div></main>}
export function Account(){return <Simple title="Account dashboard" eyebrow="ACCOUNT"/>}export function Wishlist(){return <Simple title="Wishlist" eyebrow="SAVED ITEMS"/>}
export function SearchPage({products,wish,onWish,onAdd}){return <main className="section"><div className="search"><input autoFocus placeholder="Search products, categories and stories..."/></div><div className="grid four">{products.slice(0,20).map(p=><ProductCard key={p.id} p={p} wish={wish.includes(p.id)} onWish={onWish} onAdd={onAdd}/>)}</div></main>}